spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ads_platform_db?serverTimezone=UTC
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  liquibase:
    change-log: classpath:liquibase/db.changelog-master.xml
    url: jdbc:postgresql://localhost:5432/ads_platform_db?serverTimezone=UTC
    user: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect



server:
  port: 8080

jwt:
  access-token-secret: "dGhpcyBpcyBhIDMyIGJ5dGUgc2VjcmV0IGZvciBIVzI1Ng=="
  refresh-token-secret: "dGhpcyBpcyBhIDMyIGJ5dGUgc2VjcmV0IGZvciBIVzI1Ng=="